This Fortran program is equivalent to the R package, 
except that the input & output does not go via R but 
via text files and command-line arguments. 

Its version will typically be functionally identical 
to the latest R package beta version. 